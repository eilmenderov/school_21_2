

gcc -Wall -Wextra -Werror -D BUFFER_SIZE=32 get_next_line.c get_next_line_utils.c && ./a.out | cat -e

/*
#include <stdio.h>
#include <fcntl.h>

int main(void)
{
	int fd = open ("file", O_RDONLY);
	char *line;
	int i = 1;
	while (i)
	{
		i = get_next_line(fd, &line);
		printf("i = %d %s\n", i, line);
		free (line);
	}
	return (0);
}
*/